#!/usr/bin/python3
# -*- coding: utf-8 -*-

# DataStructs
#   stack,list->list()
#   map->{}.fromkeys()
#   set->set()
#   queue->Queue()                  [import Queue]
#   PriorityQueue->PriorityQueue()  [import Queue]

# Overload Operation
# ** -> __pow__
# <  -> __lt__
# == -> __eq__

# Array bisection algorithm
# bisect.bisect_left(a, x, lo=0, hi=len(a))     [import bisect]
# bisect.bisect_right(a, x, lo=0, hi=len(a))    [import bisect]
# input use ..sys.stdin..

import sys
